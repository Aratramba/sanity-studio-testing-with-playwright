{
  "devDependencies": {
    "dotenv": "^8.2.0",
    "jest": "^26.6.3",
    "jest-playwright-preset": "^1.4.6",
    "playwright": "^1.8.1"
  },
  "scripts": {
    "test": "cd test/ && DEBUG=pw:api jest --detectOpenHandles --runInBand"
  }
}
